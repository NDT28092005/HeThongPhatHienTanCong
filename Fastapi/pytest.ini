[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Prepend tests/ to sys.path so "from conftest import ..." resolves correctly.
# Without this, pytest prepends the project root (Fastapi/) which shadows tests/
# and breaks the conftest import.
pythonpath = tests
